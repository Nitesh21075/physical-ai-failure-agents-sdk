[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "physical-ai-failure-agents-sdk"
version = "0.2.0"
description = "OpenAI Agents SDK harness for mine failure research in Isaac Sim and Reactor."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "Pillow>=10",
    "imageio-ffmpeg>=0.5",
    "numpy>=1.26",
    "openai>=1.0",
    "openai-agents>=0.4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.8",
    "httpx2>=0.1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
